[phases.setup]
nixPkgs = ['python311', 'chromium']

[phases.install]
cmds = [
  'python -m pip install --upgrade pip',
  'python -m pip install -r requirements.txt',
  'playwright install chromium'
]

[start]
cmd = "python stipe002.py"
